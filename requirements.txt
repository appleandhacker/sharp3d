# sharp3d 运行依赖
# Python 3.13 + CUDA 13.0 + Windows
#
# 安装顺序:
#   1. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#   2. pip install gsplat-*.whl  (预编译 whl 见 GitHub Releases)
#   3. pip install -e <ml-sharp路径>  (SHARP 模型库)
#   4. pip install -r requirements.txt  (其余依赖)

# ---- PyTorch (需从 PyTorch 官方源安装) ----
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch==2.13.0+cu130
torchvision==0.28.0+cu130

# ---- 推理加速 ----
onnxruntime-gpu==1.27.0
tensorrt==10.16.1.11
triton-windows==3.7.1.post27
ninja==1.13.0

# ---- 高斯渲染 (需从 GitHub Releases 安装预编译 whl) ----
# pip install gsplat-*.whl  (见 https://github.com/appleandhacker/sharp3d/releases)
gsplat==1.5.3

# ---- SHARP 模型 (本地 editable 安装) ----
# pip install -e <ml-sharp路径>
# sharp==0.1

# ---- GUI ----
PySide6==6.11.1

# ---- 核心依赖 ----
numpy==2.4.4
Pillow==12.2.0
opencv-python-headless==5.0.0.93
pillow-heif==1.4.0
imageio==2.37.4
imageio-ffmpeg==0.6.0
# 以下为 sharp (ml-sharp, editable) 声明的依赖，显式锁版本保证环境一致
scipy==1.18.0
plyfile==1.1.4
matplotlib==3.11.1
safetensors==0.8.0

# ---- 模型/推理辅助 ----
timm==1.0.28
jaxtyping==0.3.11
huggingface-hub==1.24.0

# ---- GPU 监控 ----
# nvidia-ml-py 提供 pynvml 模块（pynvml 包已废弃，勿重复安装）
nvidia-ml-py==13.610.43

# ---- ONNX 导出 ----
onnx==1.22.0
onnxscript==0.7.1

# ---- 打包分发 ----
pyinstaller==6.19.0
